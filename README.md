# Mean-Variance-Standard Deviation Calculator

This project is part of the **freeCodeCamp Data Analysis with Python** curriculum.

It implements a function that calculates the **mean, variance, standard deviation, maximum, minimum, and sum** of a 3×3 matrix using **NumPy**.

---

## 📌 Project Description

The function `calculate()`:
- Accepts a list of **9 numbers**
- Converts the list into a **3×3 NumPy array**
- Computes statistical values across:
  - Columns
  - Rows
  - The entire matrix (flattened)

If the input list contains fewer than 9 elements, the function raises a `ValueError`.

---

## 🧠 Technologies Used
- Python 3
- NumPy

---
